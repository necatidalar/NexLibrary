@model NexLibrary.Web.ViewModels.Books.BooksIndexViewModel

@{
    ViewData["Title"] = "Kitaplar";

    var dynamicColumns = Model.Books.Items
        .SelectMany(x => x.DinamikAlanlar.Keys)
        .Distinct()
        .ToList();

    var totalPages = Model.Books.PageSize <= 0
        ? 1
        : (int)Math.Ceiling(Model.Books.TotalCount / (double)Model.Books.PageSize);

    totalPages = totalPages <= 0 ? 1 : totalPages;
}

<style>
    .page-header {
        background: white;
        border-radius: 12px;
        padding: 18px 22px;
        margin-bottom: 20px;
        box-shadow: 0 1px 4px rgba(0,0,0,0.06);
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 15px;
    }

    .page-title {
        font-size: 24px;
        font-weight: 700;
    }

    .page-subtitle {
        color: #6b7280;
        margin-top: 4px;
        font-size: 14px;
    }

    .search-box {
        background: white;
        border-radius: 12px;
        padding: 16px;
        margin-bottom: 20px;
        box-shadow: 0 1px 4px rgba(0,0,0,0.06);
        display: flex;
        gap: 10px;
        align-items: center;
    }

    .search-input {
        flex: 1;
        padding: 11px 12px;
        border: 1px solid #d1d5db;
        border-radius: 8px;
        font-size: 14px;
    }

    .btn {
        border: none;
        background: #34495e;
        color: white;
        padding: 11px 16px;
        border-radius: 8px;
        font-weight: 700;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        font-size: 14px;
        white-space: nowrap;
    }

        .btn:hover {
            background: #405c78;
        }

    .btn-light {
        background: #e5e7eb;
        color: #111827;
    }

        .btn-light:hover {
            background: #d1d5db;
        }

    .alert {
        background: #fff3cd;
        color: #664d03;
        border: 1px solid #ffecb5;
        padding: 14px 16px;
        border-radius: 10px;
        margin-bottom: 18px;
    }

    .table-card {
        background: white;
        border-radius: 14px;
        box-shadow: 0 1px 4px rgba(0,0,0,0.06);
        border: 1px solid #eef0f3;
        overflow: hidden;
    }

    .table-header {
        padding: 16px 18px;
        border-bottom: 1px solid #eef0f3;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .table-title {
        font-size: 18px;
        font-weight: 700;
    }

    .table-count {
        color: #6b7280;
        font-size: 14px;
    }

    .table-wrapper {
        overflow-x: auto;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        min-width: 1000px;
    }

    th {
        background: #f9fafb;
        text-align: left;
        padding: 12px;
        font-size: 14px;
        border-bottom: 1px solid #eef0f3;
        white-space: nowrap;
    }

    td {
        padding: 12px;
        border-bottom: 1px solid #eef0f3;
        font-size: 14px;
        white-space: nowrap;
        vertical-align: middle;
    }

    .badge {
        display: inline-block;
        padding: 5px 9px;
        border-radius: 999px;
        font-size: 12px;
        font-weight: 700;
    }

    .badge-ok {
        background: #dcfce7;
        color: #166534;
    }

    .badge-passive {
        background: #e5e7eb;
        color: #374151;
    }

    .pagination {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 14px 18px;
        border-top: 1px solid #eef0f3;
        gap: 12px;
    }

    .pagination-info {
        color: #6b7280;
        font-size: 14px;
    }

    .pagination-actions {
        display: flex;
        gap: 8px;
    }

    .action-buttons {
        display: flex;
        gap: 8px;
        align-items: center;
    }

    @@media (max-width: 800px) {
        .page-header {
            flex-direction: column;
            align-items: flex-start;
        }

        .search-box {
            flex-direction: column;
            align-items: stretch;
        }

        .table-header {
            flex-direction: column;
            align-items: flex-start;
        }
    }
</style>

<div class="page-header">
    <div>
        <div class="page-title">Kitaplar</div>
        <div class="page-subtitle">Dinamik alanlarla birlikte kitap listesi</div>
    </div>

    <div style="display: flex; gap: 10px;">
        <a class="btn btn-light" asp-controller="Books" asp-action="Index">Yenile</a>
        <a class="btn" asp-controller="Books" asp-action="Create">Yeni Kitap</a>
    </div>
</div>

@if (ViewBag.ErrorMessage is not null)
{
    <div class="alert">
        @ViewBag.ErrorMessage
    </div>
}

@if (TempData["SuccessMessage"] is not null)
{
    <div class="alert" style="background:#dcfce7;color:#166534;border-color:#bbf7d0;">
        @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] is not null)
{
    <div class="alert" style="background:#fee2e2;color:#991b1b;border-color:#fecaca;">
        @TempData["ErrorMessage"]
    </div>
}

<form class="search-box" method="get" asp-controller="Books" asp-action="Index">
    <input class="search-input"
           type="text"
           name="search"
           value="@Model.Search"
           placeholder="Kitap adı, barkod, raf no veya dinamik alan ara..." />

    <input type="hidden" name="pageSize" value="@Model.PageSize" />

    <button class="btn" type="submit">Ara</button>

    <a class="btn btn-light" asp-controller="Books" asp-action="Index">Temizle</a>
</form>

<div class="table-card">
    <div class="table-header">
        <div class="table-title">Kitap Listesi</div>
        <div class="table-count">Toplam kayıt: @Model.Books.TotalCount</div>
    </div>

    <div class="table-wrapper">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Kitap Adı</th>

                    @foreach (var column in dynamicColumns)
                    {
                        <th>@column</th>
                    }

                    <th>Durum</th>
                    <th>Oluşturma Tarihi</th>
                    <th>İşlem</th>
                </tr>
            </thead>

            <tbody>
                @if (Model.Books.Items.Count == 0)
                {
                    <tr>
                        <td colspan="@(dynamicColumns.Count + 5)">Kitap kaydı bulunamadı.</td>
                    </tr>
                }
                else
                {
                    foreach (var book in Model.Books.Items)
                    {
                        <tr>
                            <td>@book.Id</td>
                            <td>@book.KitapAdi</td>

                            @foreach (var column in dynamicColumns)
                            {
                                <td>
                                    @(book.DinamikAlanlar.TryGetValue(column, out var value) ? value : "-")
                                </td>
                            }

                            <td>
                                @if (book.AktifMi)
                                {
                                    <span class="badge badge-ok">Aktif</span>
                                }
                                else
                                {
                                    <span class="badge badge-passive">Pasif</span>
                                }
                            </td>

                            <td>@book.OlusturmaTarihi.ToString("dd.MM.yyyy")</td>

                            <td>
                                <div class="action-buttons">
                                    <a class="btn btn-light"
                                           asp-controller="Books"
                                           asp-action="Edit"
                                           asp-route-id="@book.Id">
                                        Düzenle
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    <div class="pagination">
        <div class="pagination-info">
            Sayfa @Model.Books.PageNumber / @totalPages
        </div>

        <div class="pagination-actions">
            @if (Model.Books.PageNumber > 1)
            {
                <a class="btn btn-light"
                       asp-controller="Books"
                       asp-action="Index"
                       asp-route-search="@Model.Search"
                       asp-route-pageNumber="@(Model.Books.PageNumber - 1)"
                       asp-route-pageSize="@Model.PageSize">
                    Önceki
                </a>
            }

            @if (Model.Books.PageNumber < totalPages)
            {
                <a class="btn"
                       asp-controller="Books"
                       asp-action="Index"
                       asp-route-search="@Model.Search"
                       asp-route-pageNumber="@(Model.Books.PageNumber + 1)"
                       asp-route-pageSize="@Model.PageSize">
                    Sonraki
                </a>
            }
        </div>
    </div>
</div>